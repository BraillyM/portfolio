<?php 
// Gestion de la page active (par défaut 'home')
$page = isset($_GET['p']) ? $_GET['p'] : 'home';
?>
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Mathéo Brailly | Portfolio</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root { --dark-blue: #003366; --light-blue: #00AEEF; }
        body { background-color: #f4f7f6; font-family: 'Segoe UI', sans-serif; }
        .navbar { background-color: var(--dark-blue) !important; }
        .cv-section-title { 
            border: 2px solid var(--light-blue); 
            border-radius: 50px; 
            padding: 10px 40px; 
            color: var(--dark-blue); 
            font-weight: bold;
            display: inline-block;
            margin-bottom: 30px;
        }
        .card { border-radius: 20px; border: none; transition: 0.3s; }
        .card:hover { transform: translateY(-5px); box-shadow: 0 10px 20px rgba(0,0,0,0.1); }
    </style>
</head>
<body>

<?php include('navbar.php'); ?>

<div class="container mt-5 pt-4">
    <?php 
        // Sécurité : on vérifie si le fichier existe avant de l'inclure
        $allowed_pages = ['home', 'formations', 'competences', 'experiences', 'interets', 'contact'];
        if (in_array($page, $allowed_pages) && file_exists($page . '.php')) {
            include($page . '.php');
        } else {
            include('home.php');
        }
    ?>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>